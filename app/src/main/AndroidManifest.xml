<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.hs.emirim.sinyh104.stac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".firstActivity"></activity>
        <activity android:name=".firstActivity1"></activity>
        <activity android:name=".firstActivity2"></activity>
        <activity android:name=".insetting"></activity>
        <activity android:name=".store"></activity>
        <activity android:name=".diary"></activity>


    </application>

</manifest>